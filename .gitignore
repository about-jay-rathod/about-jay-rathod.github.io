# ═══════════════════════════════════════════════════════════════════════════
# 🔐 PRIVATE CREDENTIALS BRANCH - INCLUDES ALL SENSITIVE FILES
# ═══════════════════════════════════════════════════════════════════════════
# ⚠️  WARNING: This branch contains actual API keys and credentials
# ⚠️  NEVER PUSH THIS BRANCH TO PUBLIC REPOSITORIES
# ⚠️  KEEP THIS BRANCH PRIVATE AT ALL TIMES

# Only exclude large/unnecessary files - KEEP ALL CREDENTIALS
node_modules/
functions/node_modules/

# Exclude large package files (can be regenerated)
package-lock.json
yarn.lock

# Exclude large build outputs
dist/
build/
.cache/

# ═══════════════════════════════════════════════════════════════════════════
# ✅ CREDENTIALS INCLUDED (Unlike public branch)
# ═══════════════════════════════════════════════════════════════════════════
# The following files ARE INCLUDED in this private branch:
# - .env (with real API keys)
# - .firebaserc (with project details)  
# - firebase-debug.log (with debug info)
# - pglite-debug.log (with logs)
# - All configuration files with sensitive data

# ⚠️  SECURITY REMINDER: 
# This branch should only be used for:
# - Local development with all credentials
# - Private backups of configuration
# - Team members who need full access
# 
# NEVER:
# - Push to public GitHub
# - Share publicly
# - Use for production deployment (use environment variables instead)
